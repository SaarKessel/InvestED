import{describe,it,expect}from"vitest";import{assertAuthorized,createLocalUserMemory,exportUserMemory}from"./userMemory";
class S implements Storage{d=new Map<string,string>();get length(){return this.d.size}clear(){this.d.clear()}getItem(k:string){return this.d.get(k)??null}key(i:number){return [...this.d.keys()][i]??null}removeItem(k:string){this.d.delete(k)}setItem(k:string,v:string){this.d.set(k,v)}}
describe("scoped user memory",()=>{it("isolates users and scopes",()=>{const m=createLocalUserMemory(new S());m.put({id:"1",userId:"a",scope:"watchlist",value:["NVDA"],createdAt:"x",updatedAt:"x"});m.put({id:"2",userId:"b",scope:"watchlist",value:["AMD"],createdAt:"x",updatedAt:"x"});expect(m.list("a")).toHaveLength(1);expect(m.list("a")[0].value).toEqual(["NVDA"])});it("is inspectable exportable and deletable",()=>{const m=createLocalUserMemory(new S());m.put({id:"1",userId:"a",scope:"preferences",value:{lang:"he"},createdAt:"x",updatedAt:"x"});expect(exportUserMemory(m,"a")).toContain('"lang": "he"');m.clear("a");expect(m.list("a")).toEqual([])});it("denies cross-user authorization",()=>expect(()=>assertAuthorized({id:"a",displayName:"A",createdAt:"x"},"b")).toThrow("authorization_denied"));});
