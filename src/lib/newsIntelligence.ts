export type EventType="earnings"|"guidance"|"merger_acquisition"|"regulatory"|"macroeconomic"|"analyst_action"|"management_change"|"corporate_announcement"|"unknown";export interface RawNewsItem{title:string;url:string;source:string;publishedAt:string;summary?:string}
export interface NewsEvent extends RawNewsItem{id:string;symbols:string[];eventType:EventType;classificationSource:"deterministic_rules";sentiment:null;sentimentReason:"not_configured"}
const rules:Array<[EventType,RegExp]>=[["earnings",/earnings|quarterly results|תוצאות/i],["guidance",/guidance|outlook|תחזית/i],["merger_acquisition",/merger|acquisition|acquire|מיזוג|רכישת/i],["regulatory",/regulat|sec |antitrust|רגולט/i],["macroeconomic",/inflation|interest rate|fed |gdp|אינפלציה|ריבית/i],["analyst_action",/upgrade|downgrade|price target|דירוג/i],["management_change",/ceo|cfo|resign|appoint|מנכ/i]];
export function normalizeNews(item:RawNewsItem,knownSymbols:string[]):NewsEvent|null{if(!item.title.trim()||!/^https?:\/\//.test(item.url)||!item.source.trim()||Number.isNaN(Date.parse(item.publishedAt)))return null;const text=`${item.title} ${item.summary??""}`;const symbols=knownSymbols.filter(s=>new RegExp(`\\b${s.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")}\\b`,"i").test(text));const eventType=rules.find(([,re])=>re.test(text))?.[0]??"unknown";return{...item,id:`${item.source}:${item.publishedAt}:${item.title}`.toLowerCase(),symbols,eventType,classificationSource:"deterministic_rules",sentiment:null,sentimentReason:"not_configured"}}
export function normalizeNewsFeed(items:RawNewsItem[],knownSymbols:string[]){return items.map(i=>normalizeNews(i,knownSymbols)).filter((i):i is NewsEvent=>i!==null).sort((a,b)=>Date.parse(b.publishedAt)-Date.parse(a.publishedAt))}
