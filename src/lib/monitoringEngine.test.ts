import{describe,it,expect}from"vitest";import{evaluateRule,type MonitoringRule}from"./monitoringEngine";const rule:MonitoringRule={id:"r",userId:"u",symbol:"NVDA",metric:"price",operator:"above",threshold:100,enabled:true,cooldownMinutes:60};const snap={symbol:"NVDA",observedAt:"2026-01-01T00:00:00Z",values:{price:110},source:"yahoo_finance",freshness:"recent"};describe("monitoring engine",()=>{it("emits provider-independent notification event",()=>expect(evaluateRule(rule,snap,Date.parse("2026-01-01T00:00:00Z"))).toMatchObject({status:"pending",observedValue:110,source:"yahoo_finance"}));it("does not trigger unmet rules",()=>expect(evaluateRule({...rule,threshold:120},snap)).toBeNull());it("honors disabled and symbol scope",()=>{expect(evaluateRule({...rule,enabled:false},snap)).toBeNull();expect(evaluateRule(rule,{...snap,symbol:"AMD"})).toBeNull()});it("honors cooldown",()=>expect(evaluateRule({...rule,lastTriggeredAt:"2026-01-01T00:00:00Z"},{...snap,observedAt:"2026-01-01T00:10:00Z"},Date.parse("2026-01-01T00:10:00Z"))).toBeNull());it("does not trigger when data unavailable",()=>expect(evaluateRule(rule,{...snap,values:{}})).toBeNull());});
