import{describe,it,expect}from"vitest";import{classifyError,sanitizeLog,withTimeout}from"./hardening";describe("production hardening",()=>{it("classifies operational failures",()=>{expect(classifyError(new Error("provider timeout"))).toMatchObject({category:"provider_timeout",retryable:true,status:504});expect(classifyError(new Error("invalid request"))).toMatchObject({category:"validation",retryable:false})});it("redacts sensitive logs",()=>expect(sanitizeLog({apiKey:"abc",token:"def",symbol:"NVDA"})).toEqual({apiKey:"[REDACTED]",token:"[REDACTED]",symbol:"NVDA"}));it("aborts timed-out work",async()=>await expect(withTimeout(signal=>new Promise((_,reject)=>signal.addEventListener("abort",()=>reject(new Error("aborted")))),5)).rejects.toThrow("aborted"));it("preserves successful work",async()=>expect(await withTimeout(async()=>42,100)).toBe(42));});
