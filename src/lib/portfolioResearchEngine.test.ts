import{describe,it,expect}from"vitest";import{analyzePortfolio}from"./portfolioResearchEngine";describe("portfolio research",()=>{it("computes value weights cost and allocations",()=>{const r=analyzePortfolio([{symbol:"nvda",quantity:2,price:100,costBasis:80,assetClass:"stock",sector:"tech",geography:"US",currency:"USD",volatilityPct:3},{symbol:"BND",quantity:10,price:80,assetClass:"bond",sector:"fixed",geography:"US",currency:"USD",volatilityPct:1}]);expect(r.totalValue).toBe(1000);expect(r.holdings[0]).toMatchObject({symbol:"NVDA",weightPct:20,unrealizedGain:40});expect(r.allocation.assetClass).toEqual({stock:20,bond:80});expect(r.weightedVolatilityPct).toBe(1.4)});it("classifies missing metadata and risk",()=>{const r=analyzePortfolio([{symbol:"A",quantity:9,price:10},{symbol:"B",quantity:1,price:10}]);expect(r.allocation.sector).toEqual({unclassified:100});expect(r.warnings.length).toBeGreaterThan(0);expect(r.weightedVolatilityPct).toBeNull()});it("does not invent cost basis",()=>expect(analyzePortfolio([{symbol:"A",quantity:1,price:1}]).holdings[0].unrealizedGain).toBeNull());it("handles empty portfolio",()=>expect(analyzePortfolio([])).toMatchObject({totalValue:0,holdings:[],educationalOnly:true}));});
